rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // REGLAS DE DESARROLLO (TEMPORALES)
    // Permiten lectura/escritura a todos para que el login funcione
    // incluso si el usuario no ha sido creado en Authentication todavía.
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}